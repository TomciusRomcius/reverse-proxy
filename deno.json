{
  "tasks": {
    "dev": "cd reverse-proxy && deno run dev",
    "test": "deno run --allow-run tester.ts",
    "test:server": "cd tester && deno task start",
    "test:client": "cd tester-client && deno task start"
  },
  "workspace": ["./reverse-proxy", "./tester", "./tester-client"]
}
